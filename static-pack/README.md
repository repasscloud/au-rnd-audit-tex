# Static R&D audit TeX pack

This folder preserves the original manually populated LaTeX framework. It is independent of the data-driven publisher in `../publishing-pipeline/`.

On macOS, run `./test-build.zsh`. On Linux, run `./test-build.sh`. Both commands compile all seven documents and fail on any Tectonic warning or error.
